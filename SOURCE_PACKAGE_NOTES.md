# Source package notes

This archive intentionally excludes runtime metadata, debug collectors, local development logs, generated `dist/`, dependencies, and Git history. The included application source retains the managed runtime adapters required by this project’s current React/tRPC/Drizzle scaffold; those adapters are application runtime code rather than user-facing source assets.

The archive contains no customer reviews, testimonials, payment processing, or fake payment success flow. The checkout submits an order request with `paymentStatus = not_applicable`.

Use `README.clean.md` for local setup, database schema, the explicit `?demo=1` visual reference switch, and production hardening notes.
